def print_square(n):
    for i in range(1,n+1):
        line = n * '*'
        print(line)

# Implement the following function:
def linear_search(lst,val)
    for i in range(len(lst)):
        if lst[i] == val:
            return i
    return None
# n is to defined to be n = the number of the elements in lst
# n = len(lst)
# but it also depends on val, where it shows on in the lst
